# cwalk

It uses [CMake](https://cmake.org) to generate the Makefile with the following:

```bash
cmake .
make
```

Then, run the program:

```bash
./postfix
```

Example:
The input `9+5-x` will produce the result `95+2-`
